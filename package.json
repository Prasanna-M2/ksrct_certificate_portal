{
  "name": "ksrct-certificate-portal",
  "version": "1.0.0",
  "description": "Root package runner for KSRCT Student Certificate Management Portal",
  "private": true,
  "scripts": {
    "build:client": "cd client && npm install && npm run build",
    "build:server": "cd server && npm install && npx prisma db push && npx ts-node prisma/seed.ts && npm run build",
    "build": "npm run build:client && npm run build:server",
    "start": "cd server && npm start",
    "dev": "cd server && npm run dev"
  }
}
